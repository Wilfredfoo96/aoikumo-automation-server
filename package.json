{
  "name": "automation-server",
  "version": "1.0.0",
  "description": "Dedicated server for browser automation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "npx playwright install chromium && npx playwright install-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
